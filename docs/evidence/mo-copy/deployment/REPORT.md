# Node Mo-copy release checkpoint (not activated)

Source: `01fdbe12c0ffe307082d582b3fce0d5603b41a09` on `design/omnus-clinic-mo-copy`; PR https://github.com/moodyproject/omnus-clinic-in-a-box/pull/1 remains open against `main`, not merged.

Frozen 27-file `dist` manifest: `build-evidence-manifest.sha256`, SHA256 `7fbafd51b2b866244a8583be0bcb52a4b855e6a2ff06c9d0c0724d24b84fb08a`. Rebuilt dist passed `shasum -a 256 -c` 27/27, as did the staged remote files. An rsync checksum/dry-run with `--delete` returned no differences or extras. `node scripts/clinic-motion-contract.mjs`, `node scripts/pack-clinic-models.mjs --check` (5/5), `npm run lint`, `npm run typecheck`, `npm run build`, and `git diff --check` passed. Source parity with the current cold-load worktree: only `src/content/copy.ts` differs within `src`; `index.html`, `vite.config.ts` and the packed model bytes match. Eight cold-load scripts were included in the commit.

New release staged at `/home/moud/omnus-node/releases/mo-sha256-7fbafd51b2b866244a8583be0bcb52a4b855e6a2ff06c9d0c0724d24b84fb08a` via personal `moud@omnus` access. Private rollback directory `/home/moud/omnus-node/rollbacks/mo-sha256-7fbafd51b2b866244a8583be0bcb52a4b855e6a2ff06c9d0c0724d24b84fb08a/` contains the original unit, release manifest, and staged next unit. Its diff changes only the WorkingDirectory and ExecStart release paths. Immediate rollback ID remains `cold-sha256-51c6e768046d480035467c349b3efbee7b7bfcb3cf3d3c63bbd9e5cee9d6cbcf`. Before activation the protected apex/www payloads passed 20/20 against their prior baseline with identified Cloudflare HTML beacon normalization.

Activation command was DENIED BEFORE EXECUTION by unattended terminal policy: `BLOCKED: Command flagged as dangerous (stop/restart system service) but single-query mode (-q) runs without a user present to approve it`. No alternate invocation or policy bypass attempted. Read-only post-denial remote check confirms the old unit SHA256 `10f4fbff994b1222cf37b5f372eaca9e968ee9815d706ded7f0b52c02b2c4dd0` still matches `prior.service`, `omnus-node-3d.service` remains active/enabled with zero restarts, WorkingDirectory still points to the cold release, and `127.0.0.1:8082` still listens. The public Node page is still the prior copy; no Mo-copy live validation or browser acceptance was performed. No DNS/Cloudflare/tunnel/apex/www/clinical/gateway change.

Next authorized action: operator-approved Node-only service activation using the already staged release and two-path-only unit, then public 27/27 hash checks, desktop 1440 and emulated phone 390 first-open/scroll gate, one failure/Retry, protected-host 20/20 recheck. Roll back only this user service to preserved `prior.service` on material failure; verify readback. Do not call this deployed until those checks pass.
