# Mobile framing and readable six-person motion

Original implementation task `t_20779212`; current sole publisher `t_00ad3ac5`, with `t_6b630ee3` now aggregation-only per controller amendment. Branch `fix/clinic-mobile-framing-motion`. No commit, push, deployment, external message, new model, or asset replacement by builder.

## Frozen release gate: controller ACCEPT and independent technical PASS

Publisher `t_00ad3ac5` read back the controller's explicit rendered ACCEPT on its card and independent `t_c6fc3ffa` PASS. Both apply to source identity `e623fc537173caa359bc61f26138355821b5d5f3bafa752e6223be577d26e2f9` / `index-YLzO7SBc.js`, not the rejected rear-view candidate. Jarvis inspected both frozen full-phone stable sheets: visible natural upper-arm pose variation and acceptable main receptionist/visitor framing; WebKit articulation remains understated, not a prominent handwave/headnod. Existing coordinator lift/return and other accepted actor/overview evidence stand. This clears the local publication hold, not physical-phone or public rendered acceptance.

Independent report: `docs/evidence/mobile-framing-motion/validator-reception/review.md`; receipt: `validator-reception/receipt.json`. It confirms 45 source files, five build files, five sealed GLBs and 18 parent originals, plus 18 fresh ordinary-scroll staff/visitor frames in Chromium390/WebKit375 with no fresh errors and focused reverse/forms/navigation PASS. That reviewer had no image-vision capability; rendered acceptance belongs to the controller. Earlier broader matrix/recovery/grounding receipts are retained, not newly rerun.

Publication uses only the existing push-main Pages workflow and contained source/scripts/docs/ignore changes. Generated evidence, screenshots, dist and node_modules stay uncommitted. Publisher evidence will be recorded separately under `docs/evidence/mobile-framing-motion/publication/`; the independent public verifier owns the final `published/release.json` and controller-assisted public visual verdict. Do not interpret successful deployment or matching bytes as user acceptance. Reload/history-to-hero remains an observed caveat with unknown cause/regression status, not proven pre-existing or fixed. No runtime source changes are authorized in this publication phase.

## Receptionist camera candidate: implementation record (pending status superseded above)

Corrective/readiness task `t_779b8eb7` follows the controller's sole-writer amendment. The prior receptionist visual failure below remains a release HOLD until Jarvis independently inspects this frozen candidate and records an explicit visual PASS. Completion of this implementation/evidence phase is not visual acceptance. Existing `t_c6fc3ffa` owns independent evidence verification; `t_00ad3ac5` alone owns any later authorized commit/publication after the controller visual gate. No new review graph, commit or push by this writer.

Only runtime change this correction: `src/three/paths.ts` mobile positions at `.28/.38` changed from `[-0.05,1.15,0.95]` to `[-0.05,0.72,-0.28]`. The sealed receptionist rig sits at Z3.32 model units and has a 180-degree Y rotation, facing -Z; the previous +Z camera saw the chair/rear. The candidate uses a front-oblique sightline. Targets, FOV, desktop keys, other mobile keys, all gesture code, assets, seats, furniture and patient route are unchanged. Adjoining entrance/exit camera transitions necessarily interpolate to the new reception angle; their final visual acceptance belongs to the focused independent check, not a claim from geometry alone.

Evidence root: `docs/evidence/mobile-framing-motion/reception-candidate/` (ignored, never stage).

- `chromium-390-staff-stable-phone.png` and `webkit-375-staff-stable-phone.png`: four full, unscaled phone captures at `.315/.325/.345/.365`. Maximum settled camera component delta is 2.4661556363714254e-7 and 2.596811654598241e-12 respectively. These support fixed-background inspection, not automatic visual PASS.
- `initial390/` and `webkit375/`: normal-production ordinary-scroll sequences, six receptionist frames `.281/.295/.315/.325/.345/.365` and three visitor frames `.305/.325/.344` per case, plus overview capture. The historical crop coordinates in the reused native strips may not fit the new angle; judge the original full-phone frames/stable sheets rather than those crops. The first entry frame may include camera settling and is not the decisive pose comparison.
- `readiness.json`: exact source/build/five-GLB/frame SHA256 identities, deduplicated 18 actor frames (12 receptionist, 6 visitor), two engine/width configurations, no fresh page/console/network failures and five GLBs loaded each. Each browser also passed overview reverse scroll, modal open, empty validation, Escape, skip and home checks. No broad matrix rerun. Prior other-room/recovery/ground-contact receipts remain historical evidence on unchanged owning code, not newly executed tests.
- `camera-invariants.mjs`: geometric front-side prerequisite failed against original rear keys before the edit, then passed; other keys/desktop/targets/FOV unchanged and stable reception samples checked. Initial wrong relative import is retained in `camera-red.log` as harness error, not the behavioral RED. The actual assertion RED was recorded in task tool output. This diagnostic uses ignored `paths-before.ts` and is not a clean-checkout CI test.

Fresh exact commands passed: `node --experimental-strip-types docs/evidence/mobile-framing-motion/reception-candidate/camera-invariants.mjs`; `npm run lint`; `npm run typecheck`; `npm run build`; `npm run build:pages`; `node scripts/clinic-integration-assets.mjs`; `node --experimental-strip-types scripts/clinic-integration-walls.mjs`; `git diff --check`; `node docs/evidence/mobile-framing-motion/reception-candidate/artifacts.mjs`. Native commands reuse `published/native-release.mjs` with `ACTORS=Reception_Staff,Reception_Visitor`, `CASES='[["chromium",390]]' OUT=docs/evidence/mobile-framing-motion/reception-candidate/initial390`, then `CASES='[["webkit",375]]' OUT=docs/evidence/mobile-framing-motion/reception-candidate/webkit375`. Preview command is unchanged below; HTTP200 verified before capture. Node22.13.1/Vite6.4.3; Node's experimental type-strip warning remains informational.

Frozen runtime source aggregate SHA256 (ordered `git ls-files src` path/hash JSON): `e623fc537173caa359bc61f26138355821b5d5f3bafa752e6223be577d26e2f9`. Pages HTML SHA256 `4c7c64230b2a03bc550ddc50dc01c721e566547174325f0573f30d11bed0c3f3`; `index-YLzO7SBc.js` SHA256 `7f19d57b3c5188967bf763851ef7d54dc2e81d51e08071a5abd66aad703e25d7`; `index-BvKLpz6F.css` SHA256 `55dc5e131ca05fe32e938e27c49ff0827fb94fdee876d4a02c5eacc964ff6099`. All five baseline/public-source/dist model hashes match (full hashes in readiness.json). Final Pages rebuild reproduces the candidate filename after normal build.

Release audit: HEAD and fresh remote main are both `e4a4d572084c685fbc6bf380588419a7302971ea`; git index empty, no tracked dist/node_modules/generated evidence, ignore checks pass. Reviewed dirty tracked diff covers mobile resize cleanup/initial guard, safe-area/FOV/parallax camera logic, mobile keys, actor-owned quaternion overlays, native settlement assertion and ignore rule. Baseline diff is empty for manifests/lockfile, Pages workflow, App, UI/content/hooks, acceptedModel and AcceptedClinic: story/navigation/forms/recovery/default reduced-motion and model/furniture ownership remain intact. Workflow remains push-main/manual -> Node22/npm ci/lint/build:pages -> dist upload -> Pages deploy. No diagnostic browser suite runs in CI. Future publisher must recheck remote identity, explicitly stage intended source/tests/docs only and use normal fast-forward publication; no donor/main/recovery worktree access is required. Rollback remains the baseline SHA, not a reset or force-push authorization.

Implementation-time gate was Jarvis visual inspection plus independent validator handoff; both are now satisfied as recorded above. Physical-phone/Moud acceptance, sampled-not-exhaustive collision limits, patient fixed-camera labels and observed public reload/history-to-hero caveat remain open. This implementation record does not claim public candidate verification.

Corrective writer stopped owned npm/Vite PIDs21602/21631; exact process and4199 listener readback are empty. Runtime/artifacts are frozen; downstream writers must also wait for builder PID20890 to exit. `hotspot: dist/ and docs/evidence/mobile-framing-motion/` — shared workspace, sole-writer ownership released only at exit; do not rebuild concurrently with validation.

## Resumed publisher preflight: coordinator PASS, receptionist visual HOLD (prior candidate)

Publisher `t_6b630ee3` independently confirms the corrected coordinator's visible hand lift AND return in ordinary-scroll production frames. Whole-clinic phone centering and main room actor containment also pass. Publication remains held for a separate remaining `Reception_Staff` visibility failure: orange receptionist head/arm contours are essentially unchanged at native phone scale despite real bone changes. Six ordinary-scroll samples `.281/.295/.315/.325/.345/.365` in Chromium390 and WebKit375 reproduce it. The strongest full-phone comparison uses settled `.315/.325/.345/.365`, excluding first-entry camera settling as motion proof. This supersedes earlier receptionist visual PASS claims; it does not reopen the resolved coordinator objection or require a broad redesign.

Evidence root `docs/evidence/mobile-framing-motion/published/`:

- `local-native/staff-stable-phone.png`: four unscaled390px phone captures, stable desk and essentially unchanged orange receptionist silhouette.
- `local-native/chromium-390-Reception_Staff-native-strip.png` and `local-staff-webkit/webkit-375-Reception_Staff-native-strip.png`: six-pose 1:1 evidence each.
- `local-native/chromium-390-Follow_Coordinator-native-strip.png`: latest production coordinator lift/return PASS.
- `release.json`: explicitly `held_visual_failure`, no published SHA/run/public identity claim; normal local HTML/bundle/five-model hashes and exact gate counts.
- Fresh final checks: lint/types/build/Pages build/sealed five GLBs/diff hygiene PASS;8 engine-width framing cases (Chromium/WebKit375/390/430/1440,48 screenshots);6 recovery cases (normal/reduced opt-in/manual asset retry in both engines);31 native actor frames across2 configurations,12 receptionist frames, all five GLBs and zero fresh console/page/network failures. Expected injected recovery errors remain separate.
- Initial native harness `.wordmark` strict-selector failure was test-only, fixed to `button.wordmark`; final rerun passes. Original failed log retained, excluded from counts. Source runtime untouched by publisher.

Exact reproduction: start normal Pages preview with the base below, then `CASES='[["chromium",390]]' node docs/evidence/mobile-framing-motion/published/native-release.mjs`; WebKit-only staff proof uses `CASES='[["webkit",375]]' ACTORS=Reception_Staff OUT=docs/evidence/mobile-framing-motion/published/local-staff-webkit` with the same script. Read-only React renderer inspection supports actual bones; no development imports, pose writes or diagnostic query flags. Patient native sequence explicitly includes camera travel; original fixed-camera diagnostic remains separate.

Required next action is a contained receptionist-only readable sightline/gesture correction through the existing parent/controller, not another publisher or unrelated camera work. No commit or push; remote main remains `e4a4d572084c685fbc6bf380588419a7302971ea`. Candidate public verification intentionally not run after the material visual failure. Physical-phone acceptance and the existing public reload/history-to-hero caveat remain open. Publisher stopped its owned preview PIDs18333/18376; final process/port readback recorded in the card.

## Coordinator correction: independently confirmed local visual PASS

Contained follow-up `t_627b89df` resolves the coordinator visibility failure below in new local evidence, not by relabeling the original FAIL. Only `src/three/paths.ts` mobile keys `.675/.74` changed from rear `[-0.15,0.85,0.16]` to front-oblique `[-0.85,0.65,-0.78]`; target/FOV unchanged. No motion, desktop keys, other-room/overview keys, GLBs, furnishings, seats or patient-path edits. The adjoining transitions interpolate to this new angle normally, with no pose jump or autoplay.

First candidate was directly inspected at Chromium390 before the remaining cases. Existing left forearm/hand now rises into open silhouette beside the monitor and returns to lap/desk level; the chair and desk stay stationary across ordinary-scroll `.675/.685/.695/.705/.715/.725/.735`. Full phone frames and unscaled 1:1 strips were directly inspected for Chromium390, WebKit375 and WebKit430 manual low-quality retry. Main coordinator is unclipped and copy clear; peripheral furniture and other-room figures can still be cropped or under the header, as previously disclosed. This is sampled readable articulation, not physical-iPhone approval or exhaustive continuous collision certification.

Correction evidence root: `docs/evidence/mobile-framing-motion/coordinator-correction/`.

- `coordinator-before-after.png`: small labeled full-native390 two-pose comparison, original failed rear camera above / corrected front-oblique below. No magnification or crop-only readability claim.
- `normal/chromium-390-follow-sequence.png`, `normal/webkit-375-follow-sequence.png` and corresponding `*-follow-native-crops.png`; seven original full-phone `*-follow-<p>.png` frames each.
- `retry430/webkit-430-follow-sequence.png`, corresponding `*-follow-native-crops.png`, and full `*-follow-<p>.png` frames. Manual injected asset failure/retry is retained in `retry430/coordinator-results.json`; recovered backing store430 equals CSS430 (DPR1).
- `normal/coordinator-results.json`, `retry430/coordinator-results.json`, `summary.json`: exactly3 configurations /21 coordinator frames /22 other-stage smoke frames, five real GLBs each, zero fresh page/console/network errors. Initial Chromium390 candidate has its own root `initial390.log` and screenshots; excluded from final21 count.
- `normal/*-other-staff-smoke.png` and `normal/*-patient-overview-smoke.png`: focused preservation, not a new all-six aesthetic gate. Physician/review and visitor pose differences visible, patient seated plausibly grounded, overview centered and fully contained. Reception_Staff remains subtle in these smoke crops; no new regression inferred because its camera/motion is unchanged and prior accepted proof stands. Patient ordinary-scroll approach/seated snapshots have different cameras and do not replace its fixed-camera diagnostic.
- `camera-invariants.json`: desktop key text exactly unchanged; sampled coordinator camera/FOV delta at most1.1102230246251565e-16 (Float64 lerp endpoint residue, documented initial strict-equality failure and1e-12 world-unit tolerance). Fixed furniture in actual screenshots independently confirms actor motion is not camera motion.
- `verdict.json`: explicit new local visual verdict and limitations. Original `publisher-preflight/verdict.json` and its failed sequences remain untouched.

Exact correction commands (production preview explicitly includes the Pages base):

```sh
npm run build:pages
npm run preview -- --host 127.0.0.1 --port 4199 --strictPort --base=/omnus-clinic-in-a-box/
# Verified HTML200 and /omnus-clinic-in-a-box/assets/index-DNALGlcz.js200 text/javascript.
CASES='[["chromium",390]]' node docs/evidence/mobile-framing-motion/coordinator-correction/coordinator-sequence.mjs
node docs/evidence/mobile-framing-motion/coordinator-correction/crop-sequence.mjs
SMOKE=1 OUT=docs/evidence/mobile-framing-motion/coordinator-correction/normal node docs/evidence/mobile-framing-motion/coordinator-correction/coordinator-sequence.mjs
OUT=docs/evidence/mobile-framing-motion/coordinator-correction/normal node docs/evidence/mobile-framing-motion/coordinator-correction/crop-sequence.mjs
MODE=retry CASES='[["webkit",430]]' OUT=docs/evidence/mobile-framing-motion/coordinator-correction/retry430 node docs/evidence/mobile-framing-motion/coordinator-correction/coordinator-sequence.mjs
OUT=docs/evidence/mobile-framing-motion/coordinator-correction/retry430 node docs/evidence/mobile-framing-motion/coordinator-correction/crop-sequence.mjs
node docs/evidence/mobile-framing-motion/coordinator-correction/artifacts.mjs
node --experimental-strip-types docs/evidence/mobile-framing-motion/coordinator-correction/camera-invariants.mjs
npm run lint
npm run typecheck
npm run build
npm run build:pages
node scripts/clinic-integration-assets.mjs
git diff --check
```

All final commands passed. Normal Pages bundle `dist/assets/index-DNALGlcz.js`. Camera-only correction does not require a new motion/ground-contact suite; parent receipts remain applicable, not claimed as new runs. No69-journey rerun. No commit/push by this builder. Existing publisher `t_6b630ee3` independently verifies the exposed coordinator and completes its remaining release gates after builder exit. Known public reload/history caveat is unchanged. Correction builder stopped owned npm/Vite PIDs16216/16245; readback found neither process and no4198/4199 listener. HEAD remains `e4a4d572084c685fbc6bf380588419a7302971ea`; same intentionally dirty branch/worktree retained. Publisher must wait for correction builder PID15771 exit before writing.

Patient labeling correction: `scripts/clinic-visible-motion.mjs` now emits a two-line `patient-seated: fixed-camera walk/sit` / progress + viewport title. Original pair/sheet PNGs in `motion-red`, `native-final390`, `native-webkit375-optin`, `native-webkit430-retry` were corrected only within that35px title area, with pixel equality asserted everywhere else and originals retained under `coordinator-correction/label-originals/`. `before-after-six-actors.png` rebuilt from those unchanged historical poses; its old coordinator row is NOT promoted to PASS. Only the new correction sequences above resolve that failure. Additional documentation touched: this handoff and the existing plan's contained-correction section.

## Original publisher preflight: FAIL retained, locally superseded above

Independent ordinary-scroll normal-production inspection does NOT confirm the builder's all-six readable-motion verdict below. Follow_Coordinator shows small shoulder/upper-arm changes, but the rear view, torso and broad chair back hide the forearm/hand action; no clear head gesture is readable at native phone scale. Seven stage samples `.675/.685/.695/.705/.715/.725/.735` in Chromium390 and WebKit375 reproduce this, including unscaled 1:1 crops. This is a visibility failure, not a claim that the underlying rig is static. Preserve other accepted work; return only the coordinator sightline/gesture correction and patient fixed-camera evidence labeling to the existing builder/controller.

Evidence and reproduction: `docs/evidence/mobile-framing-motion/publisher-preflight/`, especially `verdict.json`, `coordinator-results.json`, and both `*-follow-native-crops.png` strips. Fourteen actual phone frames load all five GLBs with zero console/page/network failures. Independent lint/types/build/Pages build/diff hygiene/sealed-GLB checks pass. No commit/push/publication performed. Remaining release gates stopped at this material visual failure; the existing public history-to-hero caveat remains. Earlier builder PASS statements below are preserved as historical handoff claims, not publisher approval.

## Result / confidence

High confidence in the reproduced mobile defects and their local repair. The overview now fits the whole chassis with balanced margins rather than cutting both sides; room cameras target the accepted actors/workstations. Five seated people make readable scroll-timed upper-body/arm gestures; the patient's original walking, turning, sitting, standing and route remain unchanged. There is no idle autoplay. Reduced-motion default, explicit opt-in, static return and lower-quality retry remain intact.

This is Chromium152 / Playwright WebKit26.5 on macOS with mobile/touch emulation, NOT physical iPhone/Safari/Telegram acceptance. Moud retains published human acceptance. Pair images establish readable sampled poses, not exhaustive collision-free motion capture or physical-device FPS. Main actors are unclipped; close room shots still show peripheral furniture and occasional adjacent-room fragments near the header. Not every room/person is fully contained during another room's close-up. Whole-clinic containment applies to the overview; no assets were hidden to manufacture it.

## Source ownership

- `src/three/CameraRig.tsx`: responsive horizontal field, whole-footprint overview fit, measured control/copy safe area through a projection offset, eased offset changes. No per-frame DOM queries. Desktop camera path/composition unchanged; mobile cannot inherit old desktop pointer parallax.
- `src/three/paths.ts`: mobile-only stable action views at accepted cast anchors. Review uses the visually selected front-right angle to expose the gesture around the monitor; no desktop-key edits.
- `src/three/clinic/acceptedMotion.ts`: actor-scoped runtime quaternion tracks composed from immutable authored interpolants. Mixer stays the sole bone writer, only existing channels in the matching rig clip are replaced. No overlay accumulation, clocks, quality-tier branch, or patient-track changes.
- `src/scroll/JourneySection.tsx`: mobile-only ResizeObserver refreshes ScrollTrigger when actual section dimensions change. Ignore the initial unchanged-size notification. This directly fixes the viewport-height/room-selection mismatch, not general history restoration.
- Focused scripts: new `clinic-mobile-fit.mjs`, `clinic-mobile-framing.mjs`, `clinic-visible-motion.mjs`, `clinic-framing-handoff.mjs`; existing `clinic-motion-native.mjs` now waits for requested native progress AND smoothing convergence, not a stale settled value. `.gitignore` excludes generated evidence. Plan: `docs/plans/clinic-mobile-framing-motion.md`.

All five GLBs retain their sealed hashes. Rooms, furnishings, surfaces, corrected consultation computer, appliance, copy, forms, navigation and accepted patient lower-body/path assets are untouched.

## Visible motion, by actor

| Actor | Inspected pair | Readable action |
|---|---|---|
| Reception_Staff | .281 → .315 | look/upper-body acknowledgement and left-hand presentation |
| Reception_Visitor | .305 → .344 | seated response with right forearm/hand and head movement |
| physician-seated | .439 → .478 | left-hand explanation from lap toward chest, torso/head response |
| patient-seated | .387 → .43 | original approach/turn/sit, independently locked camera |
| Review_Physician | .571 → .606 | visible review acknowledgement/hand movement from front-right view |
| Follow_Coordinator | .675 → .711 | coordination gesture with head/left-arm movement |

Five seated AFTER pairs use ordinary native scrolling within stable action cameras. Patient pair is explicitly a locked-camera clip seek; actual patient native travel/reverse and foot contacts have separate regression receipts. Within each pair the view is stable; the review camera intentionally differs between BEFORE and AFTER versions. The before-motion proof uses the original motion with the initial corrected camera, not a claim that it is an untouched public framing capture.

Baseline seated surfaces moved only1.5–3.6 CSS pixels and the five pairs looked frozen. Final readable seated samples move at least17.6768px with at least351 high-contrast changed crop pixels across the three inspected configurations. Final supporting gate:12px articulated surface excursion and200 pixels changing by >24 RGB levels. The earlier6px/50px exploratory gate was tightened after inspecting the candidate; aggregation explicitly reapplies the stronger gate to all18 pairs. Neither threshold replaces visual inspection: review's earlier rear camera passed numbers and FAILED visual readability, so its camera was corrected.

## Exact evidence / tests

All paths below are relative to this worktree and ignored unless otherwise stated. Machine aggregate: `docs/evidence/mobile-framing-motion/summary.json`, generated by `node scripts/clinic-framing-handoff.mjs` with exact count/deduplication/assertions.

1. LIVE baseline: `node scripts/clinic-mobile-framing.mjs` → Chromium/WebKit ×375/390/430,6 cases. `live-before/` sheets and `results.json`. Visual RED: laterally cut overview and five imperceptible seated actors.
2. `node scripts/clinic-mobile-fit.mjs` → RED in `fit-red.log`:390px chassis x=-55.98..447.21. Corrected geometry fits at approximately20.5..369.5; full overview optically inspected, not merely a box assertion.
3. `node scripts/clinic-visible-motion.mjs` → five seated RED, patient PASS; `motion-red/` and `motion-red.log`. Intermediate failed/occluded candidates retained. `review-angles.png` is the bounded sightline comparison that selected the actual front-right camera.
4. `NATIVE=1 OUT=docs/evidence/mobile-framing-motion/native-final390 node scripts/clinic-visible-motion.mjs`; `NATIVE=1 ENGINE=webkit WIDTH=375 MODE=reduced OUT=.../native-webkit375-optin ...`; `NATIVE=1 ENGINE=webkit WIDTH=430 MODE=retry OUT=.../native-webkit430-retry ...` →3 configurations,18 actor pairs. All six named actors visually inspected in each. Seated motion is ordinary scroll; patient's fixed-camera control is marked `native:false`.
5. `BASE_URL=http://127.0.0.1:4198/ OUT=docs/evidence/mobile-framing-motion/matrix-green VERIFY=1 WIDTHS=375,390,430,1440 node scripts/clinic-mobile-framing.mjs` →8 engine/width cases,6 forward captures plus7 reverse/height-resize checks each;56 explicit reverse/resize checks, no page exceptions or overflow. All six phone sheets and desktop overview inspected. Heights844→720→900→844; header/control and copy remain clear. Initial `matrix.log` RED is retained.
6. Resize root cause:844→720 originally left cached scroll span5064px, making intended.82 read.699;900 read.874. `resize-scoped-final.log` now reads .8199/.82 at all heights. Matrix preceded the final initial-notification guard; the same resize behavior was re-exercised afterward without rerunning the entire matrix.
7. `BASE_URL=http://127.0.0.1:4198/ PHASE=framing-final MODES=normal,reduced,asset node scripts/clinic-mobile-recovery.mjs` →6/6 PASS (both engines): normal, warned reduced opt-in, failed asset/manual DPR1 retry, static return, seven static scenes, modal open/Escape. Expected injected asset request errors retained. Receipt `docs/evidence/mobile-recovery/framing-final/results.json`.
8. `node scripts/clinic-motion-validation.mjs http://127.0.0.1:4198/` → exact reverse, hold and fresh-rig errors0; foot contacts at authored samples and no stale skinned culling PASS. `docs/evidence/clinic-motion/validation/determinism-contacts.json`.
9. Final `node scripts/clinic-motion-native.mjs http://127.0.0.1:4198/` →1440/390/375 PASS; zero reload scroll delta, reverse errors <1.5e-9. `native-scoped-final.log` and `docs/evidence/clinic-motion/native/results.json`. Failed intermediate observer/restoration and stale-settlement receipts are retained. Observer is now mobile-only and ignores initial unchanged dimensions; the test waits for actual requested progress.
10. `node scripts/clinic-integration-journey.mjs http://127.0.0.1:4199/omnus-clinic-in-a-box/ docs/evidence/mobile-framing-motion/production-journey` →69/69 DOM/assets/native/reverse/history/forms/focus/validation/navigation checks on the pre-final-observer-guard production build. Final guard independently covered by item9. Final rebuilt normal Pages output was then recaptured at390 with `BASE_URL=http://127.0.0.1:4199/omnus-clinic-in-a-box/ OUT=docs/evidence/mobile-framing-motion/final-production ENGINES=chromium WIDTHS=390 node scripts/clinic-mobile-framing.mjs` →6 actual scene captures, no page errors, no QA query flags.
11. Final `npm run lint`, `npm run typecheck`, `npm run build`, `npm run build:pages`, `git diff --check`, `node scripts/clinic-integration-assets.mjs` PASS. `node --experimental-strip-types scripts/clinic-integration-walls.mjs` PASS. Final normal Pages bundle: `dist/assets/index-fvhDumKE.js`.

## Deliverable images

Absolute root: `/Users/moud/coding/.worktrees/omnus-clinic-mobile-framing-motion/docs/evidence/mobile-framing-motion/`

- `before-after-overview.png`: small, labeled native390 public-before/local-after comparison.
- `before-after-six-actors.png`: all named before/after pose pairs; patient fixed-camera exception labeled.
- `before-after-phone.png`: labeled reception, consultation, review, follow-through and overview before/after.
- `native-final390/chromium-390-actors.png`, `native-webkit375-optin/webkit-375-actors.png`, `native-webkit430-retry/webkit-430-actors.png`: full actor evidence.
- `matrix-green/`: both engines/all requested widths, reverse and short/tall viewport screenshots.
- `final-production/chromium-390-sheet.png`: final normal built page, no diagnostic flags.

## Release and rollback

Publisher must wait for builder exit, inspect the small artifacts, run focused preflight, then commit/push only source/tests/docs/ignore changes. Do NOT stage evidence, node_modules or dist. No extra review graph. Same URL: `https://moodyproject.github.io/omnus-clinic-in-a-box/`. Verify exact public SHA/Pages run/bundle/all five GLBs, native phone overview and each actor's stage, opt-in/retry, and forms. Motion follows scrolling and correctly stops on a held scroll; do not advertise autoplay.

Keep the known public reload/history-to-hero limitation open. A local fixture passing restoration is NOT proof the previous public/browser-history limitation is gone. No broad history fix or physical-phone approval is claimed.

Rollback: `e4a4d572084c685fbc6bf380588419a7302971ea`.

Tool bootstrap used local `npm ci`, then nonpersistent `npm install --no-save --package-lock=false sharp`; no package/lock changes. Playwright resolves through `/Users/moud/.hermes/hermes-agent/package.json` or `PLAYWRIGHT_PACKAGE`. Existing lockfile install reported9 audit findings; no unrelated dependency upgrade was made. Builder terminated owned server PIDs7531/12534; readback found no4198/4199 listeners or running clinic test scripts. Publisher can restart4198 dev and4199 normal Pages preview after builder PID7140 exits.
