# Clinic site integration implementation plan

Goal: put the human-accepted six-person clinic inside the existing scroll-driven appliance landing page, locally only.

Architecture: replace the old Clinic render subtree with one owned GLTF adapter. Keep the appliance, narrative timing, branding, copy, navigation and forms. Apply the donor's 0.1 scale once and CLINIC.floorY translation once; preserve the two original-person placements and the four authored room placements. Uniform closed-to-low capped walls follow scroll, reversibly. No duplicate old rooms, people or operations geometry. Use existing Three/Meshopt, no new dependency.

Authority: parent t_9a9c1572 run452 model handoff; run454 completed on explicit HUMAN ACCEPTANCE. Independent review was waived, not passed. Latest controller comment on t_c2e09f2a supersedes the older review prerequisite. No model aesthetics changes, commit, staging, push or deployment. Donor/original trees and existing4175 preview remain read-only.

1. Verify parent process exit, donor hash/provenance and exact asset inventory. Import only five runtime GLBs, current clinic fallback and license texts into public/models/3d-redesign. Record source hashes without copying giant .blend/tool/backups. Authoring source remains inspectable in the preserved donor.
2. Add failing runtime smoke/asset tests in scripts/clinic-integration*.mjs. Prove current baseline lacks accepted assets. Implement src/three/clinic/acceptedModel.ts and contained Clinic adapter. Exercise scale, six occupants, closed/partial/open/reverse walls and disposal with actual assets.
3. Add truthful load/context-loss recovery through App/Experience/StaticJourney, preserving CTA access; static illustration uses the accepted clinic fallback. Keep hero independent of asset loading. Add failure/reduced-motion tests before behavior changes.
4. Build Pages-base output; start loopback preview4180 only after free-port check. Compare actual live baseline and integrated same-pose screenshots. Adjust only necessary framing/lighting, then test real native scroll forward/reverse/reload/back,375/390/mobile-before-navigation, desktop1440 and resize. Reuse relevant existing interaction checks, not stale old-character assertions.
5. Run npm run lint, npm run typecheck, npm run build:pages, focused regressions and git diff --check. Capture motion, measured render/load limitations and explicit release manifest in docs/evidence/clinic-site-integration. Self-review exact changed files; complete implementation to release publisher, no same-card review.

Bound: usable preview or concrete blocker within30min. Maximum two material repair cycles. No physical-phone FPS claim, no hidden-intersection/clinical certification. Existing dependency vulnerabilities and large bundle/triangle limitations must be disclosed, not silently upgraded.

## Implementation handoff

- `AcceptedClinic.tsx` is now the sole clinic render subtree; old procedural files remain unmodified but are no longer imported by Experience. No old people/rooms are rendered or fetched.
- Owned GLTF loader retains five accepted assets, maps/scalars, original placements and static six-person poses. Disposal covers partial/cancelled loads and owned geometry/material/texture resources. Capped wall adaptation follows the original donor algorithm, adjusted to the translated floor, with bounded progress quantization and exact closed restoration.
- Existing camera keys, copy, forms, navigation, appliance and studio lights are unchanged. Imported PBR environment intensity is0.15 to avoid the appliance studio washing out the maps. Existing mobile no-shadow policy remains; no claim of donor-viewer lighting identity.
- Load failure,45-second load timeout, renderer error or context loss route to the complete static story and current six-person image, preserving CTA access. Hero can paint before the model payload finishes.
- Production preview command must include the base independently of the build: `npm run preview -- --host 127.0.0.1 --port 4180 --strictPort --base=/omnus-clinic-in-a-box/`. Build with `npm run build:pages`. HTML200 alone missed prefixed asset404 in the first preview invocation; the corrected actual browser/asset check passes.
- Gates: `npm run lint`, `npm run typecheck`, `npm run build:pages`, `node scripts/clinic-integration-assets.mjs`, `node --experimental-strip-types scripts/clinic-integration-walls.mjs`, `node scripts/clinic-integration-smoke.mjs` (dev4181), `node scripts/clinic-integration-failure.mjs http://127.0.0.1:4180/omnus-clinic-in-a-box/`, `node scripts/clinic-integration-journey.mjs`, `node scripts/clinic-integration-final.mjs`.
- The journey harness originally rejected valid cached304 responses and used an insufficient desktop smooth-scroll wait. Focused local/live comparison proves top=0 and identical vision navigation at.8, where existing copy is briefly faded in BOTH versions. Corrected assertions retain exact scroll targets and do not change public behavior.
- Valid evidence: `docs/evidence/clinic-site-integration/integrated-base-corrected/`, `journey-final/`, `live-baseline/`, `decisive-checks.json`. Earlier `integrated/` contains failed splash captures and is NOT proof. All evidence stays local, excluded from publication.
- User urgency amendment: no new aesthetic review/refinement or expanded proof cohorts; publisher owns final exact-tree/remote/live technical preflight. Independent review waived by explicit human direction, not passed.
- Release only exact files in local `docs/evidence/clinic-site-integration/release-manifest.json`; do not bulk-stage docs, screenshots, caches, original unused assets or giant authoring files. Donor authoring/source hashes remain in `docs/clinic-model-provenance.json`; source.blend provenance is preserved, not discarded.
- Limits: accepted reused static faces/poses and cuff blemishes; soft CG lighting; no physical-phone FPS, prolonged memory profile or exhaustive hidden-contact certification. Original navigation/copy timing is preserved. Existing npm audit reports8 vulnerabilities (2moderate,6high); dependencies/lockfile are unchanged, no opportunistic upgrade. Five GLBs total8,403,428bytes; Three bundle remains about1.135MB before compression.
- No stage/commit/push/merge/deploy performed. Donor model/source/viewer and original dirty website remain read-only; existing4175 service is preserved. Rollback base: `b24285f5dfcfa6c8fe68171da33d9d832d4e6da7`.
